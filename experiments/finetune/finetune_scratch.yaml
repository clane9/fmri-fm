classifier_kwargs:
  xavier_init: false
  norm: false

model_kwargs:
  scratch_init: true

finetune_mode: full_ft

full_ft:
  freeze_layers: null

batch_size: 32
accum_iter: 4

epochs: 30
delay_epochs: 0
head_lr_scale: 1.0

wandb: true
