# intermediate temporal patch size
t_patch_size: 4

masking: tube

model_kwargs:
  no_decode_pos: true
  no_embed_class: true
  head_init_scale: 0.0

datasets:
  hcp-train:
    buffer_size: 2000

normalize: frame
no_coord_normalize: false

seed: 3073
